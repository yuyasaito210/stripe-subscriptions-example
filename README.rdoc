An example project to test stripe setup

Prerequisites
-------------
You will need a stripe account. Set one up at http://stripe.com

Installation
------------
1. Replace the test stripe keys with your own. (config/initializers/stripe.rb)
2. $ bundle
3. $ rake db:migrate